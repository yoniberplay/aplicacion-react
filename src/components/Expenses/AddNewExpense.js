import React from "react";

 const AddNewExpenseButton = ( props ) => {

    return (
        <button onClick={props.addnewexpense}>Add New Expense</button>
      );
 }

 export default AddNewExpenseButton;